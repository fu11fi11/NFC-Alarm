import 'package:flutter/material.dart';

class AppColors {
    static const background   = Color(0xFF1A1A2E);
    static const surface      = Color(0xFF16213E);
    static const primary      = Color(0xFF0F3460);
    static const chainCard    = Color(0xFF0D1B3E);
    static const ringing      = Color(0xFF1A0000);
    static const drawerBg     = Color(0xFF16213E);
    static const drawerHeader = Color(0xFF0F3460);
}
